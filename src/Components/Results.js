/** @format */

import React from "react";
import Meaning from "../Components/Meaning";

const Result = ({ data }) => {
  return (
    <section>
      <Meaning data={data} />
    </section>
  );
};
export default Result;
